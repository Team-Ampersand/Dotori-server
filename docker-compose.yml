version: '2'
services:
  app:
    build: hippo911911/dotori-test-server:latest
    ports:
      - "8081:8080"
    links:
      - redis
  redis:
    image: redis:latest
    command: ["redis-server", "--bind", "redis", "--port", "6379"]